class_name Brick extends StaticBody2D

enum BrickType {
	BRICK
}

var index:int
var type:BrickType

func _set_brick(new_index:int, new_type:BrickType) -> void:
	self.index = new_index
	self.type = new_type

func _set_index(new_index:int) -> void:
	self.index = new_index

func get_size() -> Vector2:
	return $CollisionShape2D.get_shape().size

signal on_collision(body, other)
